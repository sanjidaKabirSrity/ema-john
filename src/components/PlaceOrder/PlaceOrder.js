import React from 'react';
import img from '../../images/giphy.gif'

const PlaceOrder = () => {
    return (
        <div style={{padding:'20px', textAlign:"center"}}>
            <img src={img} alt="gif" style={{width:"600px"}} />
        </div>
    );
};

export default PlaceOrder;